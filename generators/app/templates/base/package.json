{
  "name": "<%= appname %>",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "serverless offline --useChildProcesses --stage local",
    "lint": "tslint -p tsconfig.json -c tslint.json",
    "lint-fix": "tslint --fix -p tsconfig.json -c tslint.json",
    "test": "npx jest --coverage",
    "test-update": "npx jest --updateSnapshot"
  },
  "license": "MIT",
  "dependencies": {}
}
